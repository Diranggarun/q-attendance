{
  "name": "functions",
  "description": "Cloud Functions for q-attendance",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "engines": {
    "node": "18"
  },
  "dependencies": {
    "firebase-admin": "^11.0.0",
    "firebase-functions": "^4.3.0",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "typescript": "^5.0.4",
    "@types/node": "^20.0.0"
  }
}
